# 爸爸妈妈讲故事 — 技术架构设计文档

| 项目 | 内容 |
|------|------|
| 产品名称 | 爸爸妈妈讲故事 |
| 文档版本 | v1.0 |
| 创建日期 | 2026-08-10 |

---

## 一、系统架构总览

### 1.1 架构设计原则

- **高可用**：核心服务多副本部署，声音合成服务支持弹性扩缩容
- **低延迟**：音频流式合成+CDN分发，首包延迟<3秒
- **安全合规**：声音生物特征数据加密存储，符合《个人信息保护法》《儿童个人信息网络保护规定》
- **可扩展**：微服务架构，各模块独立迭代；AI模型可热替换升级
- **成本可控**：热门音频预合成缓存，GPU资源按需调度

### 1.2 整体架构图

```
┌──────────────────────────────────────────────────────────────────────┐
│                           客户端层 (Client)                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │  iOS App    │  │ Android App │  │  管理后台    │  │  运营CMS    │  │
│  │  (Swift)    │  │  (Kotlin)   │  │  (React)    │  │  (React)    │  │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  │
└─────────┼────────────────┼────────────────┼────────────────┼─────────┘
          │                │                │                │
          └────────────────┴───────┬────────┴────────────────┘
                                   │ HTTPS / WSS
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│                          接入层 (Gateway)                             │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │  API Gateway (Kong / APISIX)                                 │    │
│  │  - 限流熔断  - 鉴权认证  - 路由转发  - 日志审计  - SSL卸载    │    │
│  └──────────────────────────────────────────────────────────────┘    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐       │
│  │  CDN (音频   │  │  对象存储     │  │  实时通信(WebSocket) │       │
│  │   分发)      │  │  (OSS/S3)    │  │  - 合成进度推送       │       │
│  └──────────────┘  └──────────────┘  └──────────────────────┘       │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│                        业务服务层 (Microservices)                     │
│                                                                      │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐         │
│  │ 用户服务   │ │ 内容服务   │ │ 故事服务   │ │ 订单/支付  │         │
│  │ User Svc  │ │ Content Svc│ │ Story Svc │ │ Payment Svc│         │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘         │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐         │
│  │ 声音服务   │ │ 合成调度   │ │ AI故事服务 │ │ 通知服务   │         │
│  │ Voice Svc │ │ TTS Sched  │ │ AI Story   │ │ Notify Svc │         │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘         │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐                        │
│  │ 搜索服务   │ │ 推荐服务   │ │ 审核服务   │                        │
│  │ Search Svc │ │ Rec Svc    │ │ Audit Svc  │                        │
│  └────────────┘ └────────────┘ └────────────┘                        │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│                         AI 引擎层 (AI Engine)                         │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐     │
│  │              声音克隆 & TTS 引擎集群                          │     │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │     │
│  │  │ 训练Worker│ │ 合成Worker│ │ 合成Worker│ │ 合成Worker│      │     │
│  │  │ (GPU)    │ │ (GPU)    │ │ (GPU)    │ │ (GPU)    │       │     │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │     │
│  │  - 模型训练队列    - 流式TTS推理    - 情感风格控制           │     │
│  └─────────────────────────────────────────────────────────────┘     │
│  ┌─────────────────────────────────────────────────────────────┐     │
│  │              AI 故事生成引擎                                 │     │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐                     │     │
│  │  │ LLM 接入  │ │ Prompt   │ │ 内容安全  │                     │     │
│  │  │ (多模型)  │ │ 工程服务  │ │ 审核     │                     │     │
│  │  └──────────┘ └──────────┘ └──────────┘                     │     │
│  └─────────────────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│                        数据层 (Data Layer)                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ MySQL    │ │ Redis    │ │ MongoDB  │ │ Elastic- │ │ 消息队列 │  │
│  │ (主数据) │ │ (缓存)   │ │ (文档)   │ │ search   │ │ (RocketMQ│  │
│  │          │ │          │ │          │ │ (搜索)   │  │ /Kafka) │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                            │
│  │ OSS/S3   │ │ 向量数据库│ │ 时序数据库│                            │
│  │ (音频/   │ │ (Milvus) │ │ (InfluxDB│                            │
│  │  模型)   │ │ (声纹)   │ │ 监控)    │                            │
│  └──────────┘ └──────────┘ └──────────┘                            │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│                      基础设施层 (Infrastructure)                      │
│  Kubernetes集群  │  GPU节点池  │  日志(ELK)  │  监控(Prometheus)     │
│  CI/CD(ArgoCD)   │  服务网格   │  链路追踪   │  告警(AlertManager)   │
└──────────────────────────────────────────────────────────────────────┘
```

### 1.3 技术选型总表

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| iOS客户端 | Swift + SwiftUI + Combine | 原生开发，SwiftUI构建现代UI |
| Android客户端 | Kotlin + Jetpack Compose | 原生开发，声明式UI |
| API网关 | Apache APISIX | 高性能、云原生网关 |
| 微服务框架 | Go (Gin/Kratos) / Java (Spring Boot) | 核心高性能服务用Go，复杂业务用Java |
| 服务注册发现 | Nacos / Kubernetes Service | 服务治理 |
| 关系型数据库 | MySQL 8.0 | 用户、订单、内容元数据 |
| 缓存 | Redis 7.x (Cluster) | 会话、热数据、限流计数 |
| 文档数据库 | MongoDB | 故事文本、AI生成记录、用户行为日志 |
| 搜索引擎 | Elasticsearch 8.x | 故事全文检索、标签搜索 |
| 向量数据库 | Milvus | 声纹向量存储与检索、内容相似度 |
| 消息队列 | RocketMQ / Kafka | 训练任务队列、合成任务队列、异步通知 |
| 对象存储 | 阿里云OSS / MinIO | 音频文件、声音模型文件、录音原始文件 |
| CDN | 阿里云CDN / CloudFront | 音频文件全球分发加速 |
| 容器编排 | Kubernetes (K8s) | 服务编排、弹性伸缩 |
| GPU调度 | Kubernetes + NVIDIA GPU Operator | GPU资源管理 |
| CI/CD | GitLab CI + ArgoCD | 自动化构建部署 |
| 监控 | Prometheus + Grafana | 指标采集与可视化 |
| 日志 | ELK (Elasticsearch + Logstash + Kibana) | 日志聚合分析 |
| 链路追踪 | Jaeger / SkyWalking | 分布式链路追踪 |
| AI-声音克隆 | CosyVoice / XTTS v2 / 自研微调 | TTS+声音克隆核心引擎 |
| AI-故事生成 | 通义千问/文心一言 API + 开源备选 | LLM故事生成 |
| AI-内容审核 | 阿里云内容安全 / 自建审核模型 | 文本/音频内容审核 |

---

## 二、客户端架构设计

### 2.1 iOS 架构

```
┌─────────────────────────────────────────────┐
│              Presentation Layer              │
│  SwiftUI Views + ViewModels (Combine)        │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 首页 │ │ 故事 │ │ AI  │ │ 我的 │       │
│  │     │ │ 播放 │ │ 创作 │ │     │       │
│  └──────┘ └──────┘ └──────┘ └──────┘       │
│  ┌──────┐ ┌──────┐                           │
│  │ 录音 │ │儿童  │                           │
│  │ 流程 │ │模式  │                           │
│  └──────┘ └──────┘                           │
├─────────────────────────────────────────────┤
│              Domain Layer                    │
│  Use Cases + Repository Protocols            │
│  - 录音管理、声音模型管理、故事播放、AI生成    │
├─────────────────────────────────────────────┤
│              Data Layer                      │
│  Repositories + DataSources                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ 网络API  │ │ 本地DB   │ │ 文件存储  │    │
│  │ (Moya)   │ │(CoreData │ │ (音频缓存│    │
│  │          │ │ /FMDB)   │ │  离线包) │    │
│  └──────────┘ └──────────┘ └──────────┘    │
├─────────────────────────────────────────────┤
│              Common Layer                    │
│  网络监控、日志、播放器(AVFoundation)、       │
│  录音器(AVAudioEngine)、音频处理、            │
│  Keychain存储、埋点上报                       │
└─────────────────────────────────────────────┘
```

**关键模块说明**：

| 模块 | 技术方案 | 说明 |
|------|---------|------|
| UI框架 | SwiftUI + UIKit混合 | 新页面用SwiftUI，复杂播放器页可用UIKit |
| 网络层 | Moya + Alamofire | 网络请求抽象，支持拦截器、自动重试 |
| 本地存储 | CoreData (用户数据) + FileManager (音频文件) | 离线故事音频存文件系统 |
| 音频播放 | AVFoundation + AVPlayer | 支持后台播放、锁屏控制、流媒体 |
| 录音模块 | AVAudioEngine | 实时录音、PCM数据获取、实时音量/噪音检测 |
| 音频预处理 | Accelerate框架 + 自实现VAD | 降噪、静音检测、音量归一化 |
| 儿童模式 | Guided Access API + 自定义覆盖层 | 防误触、家长验证 |
| 推送通知 | APNs | 训练完成、每日推荐等 |

### 2.2 Android 架构

```
┌─────────────────────────────────────────────┐
│              Presentation Layer              │
│  Jetpack Compose + ViewModel + StateFlow     │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 首页 │ │ 故事 │ │ AI  │ │ 我的 │       │
│  │     │ │ 播放 │ │ 创作 │ │     │       │
│  └──────┘ └──────┘ └──────┘ └──────┘       │
├─────────────────────────────────────────────┤
│              Domain Layer                    │
│  UseCases + Repository Interfaces            │
│  (Kotlin Coroutines + Flow)                  │
├─────────────────────────────────────────────┤
│              Data Layer                      │
│  Repositories + DataSources                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ Retrofit │ │ Room DB  │ │ 文件存储  │    │
│  │ (API)    │ │ (本地DB) │ │ (音频缓存│    │
│  └──────────┘ └──────────┘ └──────────┘    │
├─────────────────────────────────────────────┤
│              Common Layer                    │
│  ExoPlayer(播放)、MediaRecorder(录音)、       │
│  WebRTC(音频处理)、WorkManager(后台任务)、    │
│  DataStore(配置)、埋点上报                    │
└─────────────────────────────────────────────┘
```

**关键模块说明**：

| 模块 | 技术方案 | 说明 |
|------|---------|------|
| UI框架 | Jetpack Compose | 声明式UI，与iOS SwiftUI对齐 |
| 网络层 | Retrofit2 + OkHttp4 | 网络请求，支持拦截器 |
| 本地存储 | Room + DataStore | 结构化数据+KV配置 |
| 音频播放 | ExoPlayer (Media3) | 支持后台播放、流媒体、锁屏控制 |
| 录音模块 | MediaRecorder / AudioRecord | 实时PCM录音，配合AudioProcessor做VAD |
| 音频预处理 | WebRTC NS（降噪模块） | 降噪、静音检测、回声消除 |
| 后台任务 | WorkManager | 音频下载、训练状态轮询 |
| 推送通知 | 厂商推送（华为/小米/OPPO/vivo）+ FCM | 多厂商推送聚合 |

### 2.3 客户端核心流程实现

#### 2.3.1 录音流程

```
用户点击开始录音
    │
    ▼
配置AudioSession (iOS) / AudioManager (Android)
- 设置类别: PlayAndRecord
- 设置采样率: 24000Hz (匹配TTS模型输入)
- 设置位深: 16bit PCM
- 开启回声消除、自动增益控制
    │
    ▼
开始录音循环 (逐帧获取PCM数据)
    │
    ├──► 实时VAD检测（是否有人声）
    ├──► 实时音量计算（dBFS）
    ├──► 实时噪音水平估计
    ├──► 波形可视化数据输出
    │
    ▼
每完成一句引导文本录音
    │
    ├──► 质量检测：信噪比SNR ≥ 15dB？
    ├──► 质量检测：有效语音时长 ≥ 2秒？
    ├──► 质量检测：削波检测（无溢出）？
    ├──► 不合格 → 提示重录
    └──► 合格 → 保存为WAV/FLAC，进入下一句
    │
    ▼
全部录制完成
    │
    ├──► 本地音频后处理：降噪(RNNoise)、归一化、裁剪首尾静音
    ├──► 打包为ZIP（含元数据JSON：采样率、时长、文本标注）
    └──► 分片上传至OSS（断点续传）
```

#### 2.3.2 音频播放流程（流式合成）

```
用户选择故事+声音 → 点击播放
    │
    ▼
检查本地缓存
    ├──► 已缓存 → 直接本地播放
    └──► 未缓存 → 发起合成请求
    │
    ▼
建立WebSocket连接 → 服务端开始流式合成
    │
    ├──► 接收首包音频数据（<3秒）
    ├──► 写入临时缓冲区（预缓冲2秒）
    ├──► 开始播放（AVPlayer/ExoPlayer播放流式数据）
    ├──► 持续接收音频chunk → 追加到播放队列
    └──► 合成完成 → 保存完整音频到本地缓存
    │
    ▼
播放过程
    ├──► 支持后台播放（AudioSession配置）
    ├──► 支持锁屏控制（NowPlayingInfo / MediaSession）
    ├──► 支持蓝牙耳机/线控控制
    ├──► 定时关闭：监听播放进度，到点淡出停止
    └──► 断点续播：记录播放位置到本地
```

---

## 三、服务端架构设计

### 3.1 微服务拆分

```
┌─────────────────────────────────────────────────────────────┐
│                     API Gateway                              │
│              (统一入口、鉴权、限流、路由)                      │
└──────┬──────┬──────┬──────┬──────┬──────┬──────┬───────────┘
       │      │      │      │      │      │      │
       ▼      ▼      ▼      ▼      ▼      ▼      ▼
   ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐
   │用户  ││内容  ││故事  ││声音  ││合成  ││AI故事││支付  │
   │服务  ││服务  ││服务  ││服务  ││调度  ││服务  ││服务  │
   │      ││      ││      ││      ││服务  ││      ││      │
   └──┬───┘└──┬───┘└──┬───┘└──┬───┘└──┬───┘└──┬───┘└──┬───┘
      │       │       │       │       │       │       │
      └───────┴───────┴───┬───┴───────┴───────┴───────┘
                          │
                    ┌─────▼──────┐
                    │  消息队列   │
                    │ (RocketMQ) │
                    └─────┬──────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
    ┌──────────┐    ┌──────────┐    ┌──────────┐
    │训练任务  │    │合成任务  │    │通知任务  │
    │Consumer  │    │Consumer  │    │Consumer  │
    └────┬─────┘    └────┬─────┘    └──────────┘
         │               │
         ▼               ▼
    ┌──────────────────────────────────────┐
    │         AI Engine GPU Cluster         │
    │  训练Worker Pool  │  合成Worker Pool  │
    └──────────────────────────────────────┘
```

### 3.2 各服务职责

#### 3.2.1 用户服务 (User Service)

| 项目 | 说明 |
|------|------|
| 职责 | 用户注册登录、账号管理、家庭成员档案、儿童信息管理 |
| 技术栈 | Go (Gin) + MySQL + Redis |
| 核心接口 | 手机号验证码登录、微信/Apple ID登录、家庭档案CRUD、孩子档案CRUD |
| 数据表 | users、family_profiles、children_profiles、user_devices |

#### 3.2.2 内容服务 (Content Service)

| 项目 | 说明 |
|------|------|
| 职责 | 故事内容管理、分类标签、内容审核、CMS后台 |
| 技术栈 | Java (Spring Boot) + MySQL + Elasticsearch + MongoDB |
| 核心接口 | 故事列表、故事详情、分类树、搜索、收藏、CMS内容CRUD |
| 数据表 | stories、story_categories、story_tags、story_collections、user_favorites |
| 搜索 | Elasticsearch全文检索，支持标题/内容/标签多字段搜索，支持年龄/时长/分类过滤 |

#### 3.2.3 声音服务 (Voice Service)

| 项目 | 说明 |
|------|------|
| 职责 | 录音文件管理、声音模型生命周期管理、声纹验证 |
| 技术栈 | Go (Gin) + MySQL + OSS + Milvus |
| 核心接口 | 录音上传(预签名URL)、创建训练任务、模型列表、模型试听、模型删除、追加训练 |
| 数据表 | voice_models、voice_training_tasks、voice_samples |
| 关键逻辑 | 录音文件校验（格式/时长/完整性）、训练任务状态机管理、声纹向量存储到Milvus用于身份验证 |

**声音模型状态机**：
```
[待上传] → [上传中] → [已上传] → [训练排队中] → [训练中] → [训练完成/训练失败]
                                                         │
                                                         ▼
                                                    [可用] ←→ [追加训练中]
                                                         │
                                                         ▼
                                                    [已删除]
```

#### 3.2.4 合成调度服务 (TTS Scheduler Service)

| 项目 | 说明 |
|------|------|
| 职责 | 接收合成请求、任务调度、GPU资源管理、流式合成、音频缓存 |
| 技术栈 | Go (Gin) + Redis + RocketMQ + OSS |
| 核心接口 | 发起合成(WebSocket流式)、合成进度查询、试听合成、批量合成 |
| 数据表 | tts_tasks、tts_cache |
| 关键逻辑 | 合成任务优先级队列（试听>播放>预合成）、GPU Worker负载均衡、缓存命中判断、流式音频chunk推送 |

**合成任务优先级**：
```
P0: 试听合成（30秒片段，用户等待中）
P1: 在线播放流式合成（用户等待中）
P2: 批量预合成（热门故事+活跃用户声音，后台任务）
P3: 离线下载合成（用户主动下载，后台排队）
```

#### 3.2.5 AI故事服务 (AI Story Service)

| 项目 | 说明 |
|------|------|
| 职责 | AI故事生成、Prompt管理、故事模板、生成历史 |
| 技术栈 | Python (FastAPI) + MongoDB + Redis |
| 核心接口 | 生成故事、重新生成、编辑故事、保存故事、我的故事库 |
| 数据表 | ai_stories、story_templates、story_generation_logs |
| 关键逻辑 | Prompt组装与模板管理、多LLM负载均衡与降级、生成后处理（分段/注音/标点优化）、内容安全审核 |

#### 3.2.6 支付服务 (Payment Service)

| 项目 | 说明 |
|------|------|
| 职责 | 会员订阅、订单管理、支付渠道对接、权益管理 |
| 技术栈 | Java (Spring Boot) + MySQL |
| 核心接口 | 创建订单、支付回调、订阅状态查询、会员权益判断、退款 |
| 数据表 | orders、subscriptions、payment_transactions、user_entitlements |
| 对接渠道 | Apple IAP、Google Play、微信支付、支付宝 |

#### 3.2.7 通知服务 (Notification Service)

| 项目 | 说明 |
|------|------|
| 职责 | 推送通知、短信、消息中心 |
| 技术栈 | Go + RocketMQ Consumer |
| 核心逻辑 | 训练完成推送、每日推荐推送、订阅到期提醒、运营消息 |
| 对接渠道 | APNs、FCM、厂商推送、阿里云短信 |

#### 3.2.8 搜索服务 (Search Service)

| 项目 | 说明 |
|------|------|
| 职责 | 故事搜索、搜索推荐、热词管理 |
| 技术栈 | Go + Elasticsearch |
| 核心逻辑 | 全文检索、搜索建议(Completion Suggester)、搜索热词、搜索结果排序 |

#### 3.2.9 推荐服务 (Recommendation Service)

| 项目 | 说明 |
|------|------|
| 职责 | 个性化故事推荐、相关推荐 |
| 技术栈 | Python + Redis + Milvus |
| 核心逻辑 | 基于年龄的规则推荐、基于协同过滤的个性化推荐、基于内容向量的相似推荐 |

#### 3.2.10 审核服务 (Audit Service)

| 项目 | 说明 |
|------|------|
| 职责 | AI生成内容审核、用户举报处理 |
| 技术栈 | Python + 第三方内容安全API |
| 核心逻辑 | 文本敏感词检测、音频内容审核、用户举报工单流转 |

---

## 四、声音克隆引擎详细设计

### 4.1 引擎选型对比

| 方案 | 类型 | 中文效果 | 少样本克隆 | 情感控制 | 流式推理 | 开源 | 推荐阶段 |
|------|------|---------|-----------|---------|---------|------|---------|
| **CosyVoice** (阿里) | 开源 | ★★★★★ | ★★★★★ (3s极速克隆) | ★★★★ | ★★★ | 是 | **MVP首选** |
| **XTTS v2** (Coqui) | 开源 | ★★★★ | ★★★★ (6s) | ★★★ | ★★★★ | 是 | 备选/多语言 |
| **ChatTTS** | 开源 | ★★★★★ | ★★ (需微调) | ★★★★★ | ★★★ | 是 | 情感增强 |
| **VALL-E X** (微软) | 开源 | ★★★★ | ★★★★ | ★★★ | ★★ | 是 | 研究参考 |
| **商用API** (讯飞/火山等) | 商用 | ★★★★★ | ★★★★ | ★★★★ | ★★★★★ | 否 | 快速上线备选 |

**推荐方案**：MVP阶段采用 **CosyVoice** 作为主力引擎，理由：
1. 阿里开源，中文效果顶尖
2. 支持3秒极速声音克隆（Zero-Shot）和少样本克隆（Few-Shot）
3. 支持情感控制、方言、多语言
4. 社区活跃，持续更新
5. 可自部署，数据不出域，安全可控

### 4.2 声音克隆流水线

```
用户录音样本 (20-50句WAV, 24kHz, 16bit)
    │
    ▼
┌─────────────────────────────────────┐
│        音频预处理 Pipeline           │
│  1. 格式统一：重采样到24kHz/单声道   │
│  2. VAD精切：WebRTC VAD精确切分人声  │
│  3. 降噪：RNNoise / DeepFilterNet   │
│  4. 音量归一化：LUFS归一化到-23LUFS  │
│  5. 去混响：可选（如录音环境差）      │
│  6. 质量过滤：SNR<15dB的片段丢弃     │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│        特征提取                      │
│  1. 提取说话人嵌入 (Speaker         │
│     Embedding) — 3秒模式用此        │
│  2. 或：微调模式 — 用10-15分钟      │
│     样本做LoRA/Adapter微调          │
└─────────────────┬───────────────────┘
                  │
        ┌─────────┴─────────┐
        ▼                   ▼
┌──────────────┐    ┌──────────────┐
│  快速模式     │    │  高质量模式   │
│  (Zero-Shot) │    │  (Few-Shot)  │
│              │    │              │
│  提取Speaker │    │  LoRA微调     │
│  Embedding   │    │  (10-20min)  │
│  向量即可    │    │  训练Adapter  │
│  无需训练    │    │  获得专属模型  │
│  即时可用    │    │  效果更稳定   │
└──────┬───────┘    └──────┬───────┘
       │                   │
       ▼                   ▼
┌─────────────────────────────────────┐
│        模型/向量存储                  │
│  - Speaker Embedding: 256维向量     │
│    → 存Milvus + MySQL元数据         │
│  - LoRA Adapter: ~50MB模型文件      │
│    → 存OSS + 本地SSD缓存            │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│        TTS 合成推理                  │
│  输入: 文本 + Speaker Embedding/    │
│        LoRA权重 + 情感参数           │
│  输出: 24kHz PCM音频流              │
│  方式: 流式推理 (Streaming Inference)│
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│        音频后处理                    │
│  1. 音频增强：轻微降噪、响度匹配     │
│  2. 添加数字水印（不可听声纹水印）   │
│  3. 编码为AAC/MP3                   │
│  4. 流式推送给客户端 / 存OSS        │
└─────────────────────────────────────┘
```

### 4.3 声音克隆引擎部署架构

```
┌─────────────────────────────────────────────────────┐
│              TTS Inference Cluster                   │
│                                                      │
│  ┌─────────────────────────────────────────────┐    │
│  │  Load Balancer (gRPC)                       │    │
│  │  - 轮询/最少连接数调度合成Worker              │    │
│  │  - 模型预热管理                              │    │
│  └──────────────────┬──────────────────────────┘    │
│                     │                                │
│  ┌──────────────────▼──────────────────────────┐    │
│  │  GPU Worker Pool (NVIDIA A10/A100/L40)      │    │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │    │
│  │  │Worker│ │Worker│ │Worker│ │Worker│ ...  │    │
│  │  │ :8080│ │ :8081│ │ :8082│ │ :8083│      │    │
│  │  └──────┘ └──────┘ └──────┘ └──────┘      │    │
│  │                                              │    │
│  │  每个Worker:                                  │    │
│  │  - Triton Inference Server / vLLM            │    │
│  │  - 加载基础TTS模型(GPU显存)                   │    │
│  │  - 动态加载/卸载LoRA权重(SSD缓存→显存)        │    │
│  │  - Speaker Embedding缓存(Redis/本地内存)     │    │
│  │  - 支持并发推理 (Dynamic Batching)            │    │
│  └──────────────────────────────────────────────┘    │
│                                                      │
│  ┌─────────────────────────────────────────────┐    │
│  │  Training Worker Pool (按需弹性伸缩)          │    │
│  │  - 空闲时缩容到0，有训练任务时自动扩容        │    │
│  │  - LoRA微调训练任务                          │    │
│  │  - 训练完成后模型推送到OSS                    │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

### 4.4 合成接口设计（WebSocket流式）

```
客户端 → 服务端 (合成请求):
{
  "task_id": "uuid",
  "voice_model_id": "vm_xxx",
  "story_id": "st_xxx",           // 预置故事，或
  "text": "故事全文...",           // 自定义文本
  "config": {
    "speed": 1.0,                 // 语速 0.7-1.3
    "pitch": 0,                   // 音调 -3~+3
    "emotion": "gentle",          // gentle/happy/calm/mysterious
    "quality": "standard",        // standard/high
    "format": "aac",              // aac/mp3/pcm
    "sample_rate": 24000
  }
}

服务端 → 客户端 (流式响应):
1. 合成开始:
   {"type": "start", "task_id": "uuid", "total_chunks": 120}

2. 音频数据 (持续推送):
   {"type": "audio", "chunk_index": 0, "data": "<base64 AAC frame>"}
   {"type": "audio", "chunk_index": 1, "data": "<base64 AAC frame>"}
   ...

3. 合成进度:
   {"type": "progress", "chunk_index": 45, "total_chunks": 120}

4. 合成完成:
   {"type": "done", "task_id": "uuid", "audio_url": "https://oss.../xxx.aac", "duration_ms": 185000}

5. 合成失败:
   {"type": "error", "task_id": "uuid", "code": "TTS_001", "message": "模型加载失败"}
```

### 4.5 音频缓存策略

```
┌─────────────────────────────────────────────┐
│              缓存层级设计                     │
│                                              │
│  L1: 客户端本地缓存                          │
│  - 已播放过的故事音频存本地文件系统            │
│  - 按 voice_model_id + story_id 做Key        │
│  - 容量上限2GB，LRU淘汰                      │
│                                              │
│  L2: CDN缓存                                 │
│  - 热门故事+热门声音的合成音频预推CDN         │
│  - TTL: 7天，命中则直接返回CDN               │
│                                              │
│  L3: 服务端Redis缓存                         │
│  - 合成音频元数据缓存（URL、时长、大小）       │
│  - Speaker Embedding向量缓存                 │
│  - TTL: 30天                                │
│                                              │
│  L4: OSS持久存储                             │
│  - 所有合成过的音频持久化存储                 │
│  - 按日期/voice_model_id/story_id组织路径    │
│  - 低频访问转低频存储类型                     │
└─────────────────────────────────────────────┘
```

**预合成策略**：
- 每日凌晨定时任务：为每个活跃用户（近7天有使用）的声音模型，预合成Top 50热门故事
- 新故事上线后，自动为活跃用户的声音模型排队合成
- 用户首次试听后，自动触发完整故事的后台合成

---

## 五、AI故事生成引擎设计

### 5.1 架构设计

```
┌─────────────────────────────────────────────────┐
│              AI Story Generation Engine           │
│                                                   │
│  ┌─────────────┐                                  │
│  │  API Layer   │  FastAPI接口，接收生成请求       │
│  │  (FastAPI)  │                                  │
│  └──────┬──────┘                                  │
│         │                                         │
│         ▼                                         │
│  ┌─────────────┐                                  │
│  │ Prompt      │  根据用户输入组装Prompt           │
│  │ Builder     │  - 系统角色设定                  │
│  │             │  - 故事参数（主题/角色/风格）     │
│  │             │  - 长度控制                      │
│  │             │  - 安全约束                      │
│  └──────┬──────┘                                  │
│         │                                         │
│         ▼                                         │
│  ┌─────────────┐     ┌─────────────────────┐     │
│  │ LLM Router  │────►│  Primary LLM        │     │
│  │ (负载均衡/   │     │  (通义千问/文心一言) │     │
│  │  降级)      │────►│  Secondary LLM      │     │
│  │             │     │  (GPT-4o-mini/DeepSeek)│   │
│  │             │────►│  Fallback LLM       │     │
│  │             │     │  (本地开源Qwen2-7B) │     │
│  └──────┬──────┘     └─────────────────────┘     │
│         │                                         │
│         ▼                                         │
│  ┌─────────────┐                                  │
│  │ Post Process│  1. 文本清洗（去除AI套话）       │
│  │             │  2. 自动分段（适合TTS的段落）     │
│  │             │  3. 标点优化（TTS友好的标点）     │
│  │             │  4. 生僻字注音标注（拼音）        │
│  │             │  5. 预估时长                     │
│  └──────┬──────┘                                  │
│         │                                         │
│         ▼                                         │
│  ┌─────────────┐                                  │
│  │ Content     │  1. 敏感词检测                   │
│  │ Safety Check│  2. 暴力/恐怖/不当内容过滤       │
│  │             │  3. 儿童适宜性评分               │
│  │             │  4. 不通过则重新生成(最多2次)     │
│  └──────┬──────┘                                  │
│         │                                         │
│         ▼                                         │
│  ┌─────────────┐                                  │
│  │ Storage     │  存入MongoDB，返回给客户端       │
│  └─────────────┘                                  │
└─────────────────────────────────────────────────┘
```

### 5.2 Prompt模板设计

```
【系统Prompt】
你是一位专业的儿童故事作家，擅长为3-10岁儿童创作温暖、有趣、富有教育意义的故事。
请严格遵守以下规则：
1. 故事内容必须适合儿童，不得包含暴力、恐怖、血腥、歧视等不当内容
2. 语言简单易懂，适合朗读，句子不宜过长
3. 故事应有积极正面的价值观
4. 避免使用复杂的成语和生僻字（如必须使用，请标注拼音）
5. 故事中适当使用拟声词和对话，增加趣味性
6. 输出格式为纯文本故事正文，不要添加标题以外的解释性文字
7. 故事结尾可以自然收尾，不需要刻意总结道理

【用户Prompt模板】
请为我创作一个儿童故事，要求如下：
- 主题：{theme}（如：勇敢、分享、友谊）
- 主角名字：{child_name}（用这个名字作为故事主角）
- 主角性格：{character_trait}
- 故事场景：{scene}
- 故事长度：{length}（短篇约3分钟/中篇约6分钟/长篇约10分钟）
- 故事风格：{style}（温馨治愈/冒险刺激/幽默搞笑/科普教育）
- 配角：{supporting_roles}
- 教育寓意：{moral}（可选）

请直接输出故事正文：
```

### 5.3 故事文本后处理规则

| 处理项 | 规则 |
|--------|------|
| 分段 | 按语义自然分段，每段2-5句话，适合TTS呼吸停顿 |
| 标点 | 将分号改为逗号或句号；省略号统一为"……"；添加适当停顿标记 |
| 数字 | 阿拉伯数字转为中文读法（如"3"→"三"，"100"→"一百"） |
| 生僻字 | 对超出常用3500字范围的字标注拼音（如"貔貅(pí xiū)"） |
| 英文 | 英文单词标注中文读音提示或替换为中文表达 |
| AI套话清洗 | 去除"好的，这是一个故事"、"希望你喜欢"等AI常见回复 |
| 时长估算 | 按中文朗读速度约4字/秒估算故事时长 |

---

## 六、数据存储设计

### 6.1 核心数据模型

#### 6.1.1 MySQL核心表

```sql
-- 用户表
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    phone VARCHAR(20) UNIQUE COMMENT '手机号',
    wechat_openid VARCHAR(64) UNIQUE COMMENT '微信openid',
    apple_id VARCHAR(64) UNIQUE COMMENT 'Apple ID',
    nickname VARCHAR(64) NOT NULL,
    avatar_url VARCHAR(512),
    membership_type TINYINT DEFAULT 0 COMMENT '0免费 1月卡 2季卡 3年卡 4家庭卡',
    membership_expire_at DATETIME COMMENT '会员到期时间',
    status TINYINT DEFAULT 1 COMMENT '1正常 0禁用',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_membership_expire (membership_expire_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 孩子档案表
CREATE TABLE children_profiles (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    nickname VARCHAR(32) NOT NULL COMMENT '孩子小名',
    gender TINYINT COMMENT '0女 1男',
    birthday DATE COMMENT '生日',
    interests JSON COMMENT '兴趣标签',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 声音模型表
CREATE TABLE voice_models (
    id VARCHAR(32) PRIMARY KEY COMMENT 'vm_xxx',
    user_id BIGINT NOT NULL,
    name VARCHAR(32) NOT NULL COMMENT '如"爸爸的声音"',
    gender TINYINT COMMENT '0女 1男',
    clone_mode TINYINT COMMENT '1快速(zero-shot) 2高质量(few-shot)',
    status TINYINT DEFAULT 0 COMMENT '0训练中 1可用 2失败 3已删除',
    speaker_embedding_id VARCHAR(64) COMMENT 'Milvus向量ID',
    lora_model_path VARCHAR(512) COMMENT 'LoRA模型OSS路径',
    sample_duration_ms INT COMMENT '录音总时长(毫秒)',
    training_progress TINYINT DEFAULT 0 COMMENT '训练进度0-100',
    training_error_msg VARCHAR(512),
    is_default TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 故事表
CREATE TABLE stories (
    id VARCHAR(32) PRIMARY KEY COMMENT 'st_xxx',
    title VARCHAR(128) NOT NULL,
    cover_url VARCHAR(512),
    category_id BIGINT NOT NULL,
    tags JSON COMMENT '标签数组',
    min_age TINYINT DEFAULT 3 COMMENT '最小适合年龄',
    max_age TINYINT DEFAULT 10 COMMENT '最大适合年龄',
    duration_ms INT COMMENT '预估时长(毫秒)',
    word_count INT COMMENT '字数',
    author VARCHAR(64) COMMENT '作者/来源',
    is_premium TINYINT DEFAULT 0 COMMENT '是否付费内容',
    play_count INT DEFAULT 0 COMMENT '播放次数',
    favorite_count INT DEFAULT 0 COMMENT '收藏次数',
    status TINYINT DEFAULT 1 COMMENT '0下架 1上架',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_category (category_id),
    INDEX idx_age (min_age, max_age),
    INDEX idx_play_count (play_count)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- TTS合成任务表
CREATE TABLE tts_tasks (
    id VARCHAR(32) PRIMARY KEY COMMENT 'tts_xxx',
    user_id BIGINT NOT NULL,
    voice_model_id VARCHAR(32) NOT NULL,
    story_id VARCHAR(32) COMMENT '预置故事ID，与text二选一',
    input_text_hash VARCHAR(64) COMMENT '自定义文本hash',
    config JSON COMMENT '合成配置(speed/pitch/emotion/quality)',
    status TINYINT DEFAULT 0 COMMENT '0排队 1合成中 2完成 3失败',
    audio_url VARCHAR(512) COMMENT '合成音频OSS URL',
    audio_size BIGINT COMMENT '音频文件大小(bytes)',
    duration_ms INT COMMENT '音频时长',
    cache_hit TINYINT DEFAULT 0 COMMENT '是否命中缓存',
    error_msg VARCHAR(512),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    completed_at DATETIME,
    INDEX idx_user_voice_story (user_id, voice_model_id, story_id),
    INDEX idx_status_created (status, created_at),
    INDEX idx_text_hash (input_text_hash)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 订单表
CREATE TABLE orders (
    id VARCHAR(32) PRIMARY KEY COMMENT 'ord_xxx',
    user_id BIGINT NOT NULL,
    order_type TINYINT COMMENT '1月卡 2季卡 3年卡 4家庭卡 5高级克隆包',
    amount DECIMAL(10,2) NOT NULL,
    pay_channel VARCHAR(32) COMMENT 'iap/wechat/alipay',
    pay_status TINYINT DEFAULT 0 COMMENT '0待支付 1已支付 2已退款',
    transaction_id VARCHAR(128) COMMENT '第三方交易号',
    paid_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_pay_status (pay_status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### 6.1.2 MongoDB文档设计

```javascript
// 故事正文文档（大文本，用MongoDB）
{
  _id: ObjectId,
  story_id: "st_xxx",           // 关联MySQL stories.id
  title: "三只小猪",
  content: [                     // 分段存储，便于TTS逐段合成
    { paragraph_index: 0, text: "从前，在一个美丽的小村庄里..." },
    { paragraph_index: 1, text: "猪妈妈有三个猪宝宝..." },
    ...
  ],
  pinyin_annotations: {          // 生僻字注音映射
    "貔貅": "pí xiū"
  },
  tts_marks: [                   // TTS停顿/情感标记
    { position: 120, type: "pause", duration: 500 },
    { position: 350, type: "emotion", value: "happy" }
  ],
  version: 1,
  updated_at: ISODate
}

// AI生成故事文档
{
  _id: ObjectId,
  user_id: NumberLong(12345),
  child_profile_id: "cp_xxx",
  title: "小明的太空冒险",
  theme: "勇敢",
  style: "冒险刺激",
  content: [ ... ],              // 同故事正文格式
  generation_params: {
    theme: "勇敢",
    child_name: "小明",
    character: "好奇",
    scene: "太空",
    length: "medium",
    style: "adventure"
  },
  llm_model: "qwen-max",
  prompt_tokens: 800,
  completion_tokens: 1200,
  is_saved: true,                // 是否保存到我的故事库
  is_shared: false,              // 是否公开分享
  audit_status: 1,               // 0待审核 1通过 2不通过
  created_at: ISODate
}

// 用户播放历史
{
  _id: ObjectId,
  user_id: NumberLong(12345),
  child_profile_id: "cp_xxx",
  story_id: "st_xxx",
  voice_model_id: "vm_xxx",
  ai_story_id: ObjectId,         // AI故事时使用
  play_position_ms: 45000,       // 上次播放位置
  play_duration_ms: 120000,      // 本次播放时长
  completed: false,              // 是否播放完
  played_at: ISODate,
  created_at: ISODate
}
```

#### 6.1.3 Redis缓存Key设计

```
# 用户会话
user:session:{token}          -> UserInfo JSON, TTL=7天

# 声音模型Speaker Embedding缓存
voice:embedding:{model_id}    -> 256维float32向量, TTL=24小时

# TTS合成结果缓存
tts:cache:{model_id}:{text_hash}:{config_hash} -> {audio_url, duration_ms}, TTL=30天

# 热门故事列表
story:hot:{category}:{age}    -> StoryId列表, TTL=1小时

# 每日推荐
story:daily:{user_id}         -> StoryId列表, TTL=24小时

# 用户当日AI生成次数
ratelimit:ai_gen:{user_id}    -> 计数器, TTL=到次日0点

# 用户当日听故事次数
ratelimit:story_play:{user_id}-> 计数器, TTL=到次日0点

# 合成任务队列状态
tts:task:{task_id}            -> 任务状态JSON, TTL=1小时

# 分布式锁
lock:voice_training:{user_id} -> 1, TTL=30分钟
```

### 6.2 对象存储(OSS)路径设计

```
oss://story-app-bucket/
├── audio/                          # 合成音频文件
│   └── {yyyy}/{mm}/{dd}/
│       └── {voice_model_id}/
│           └── {story_id_or_hash}/
│               └── {quality}.aac   # standard.aac / high.aac
│
├── voice-samples/                  # 用户录音原始文件
│   └── {user_id}/
│       └── {voice_model_id}/
│           ├── sample_001.wav
│           ├── sample_002.wav
│           └── manifest.json       # 录音元数据
│
├── voice-models/                   # 训练好的声音模型
│   └── {voice_model_id}/
│       ├── speaker_embedding.bin   # Zero-Shot模式的向量
│       ├── lora_adapter.safetensors# Few-Shot模式的LoRA权重
│       └── model_meta.json         # 模型元数据
│
├── stories/                        # 预置故事音频（专业主播版本）
│   └── {story_id}/
│       └── narrator.aac
│
└── app/                            # App安装包等
    ├── ios/
    └── android/
```

---

## 七、安全设计

### 7.1 声音生物特征安全

| 安全措施 | 实现方式 |
|---------|---------|
| 传输加密 | 录音文件上传全程HTTPS/TLS 1.3 |
| 存储加密 | 录音文件和模型文件OSS服务端加密(AES-256)，Speaker Embedding加密存储 |
| 访问控制 | 声音模型仅所属用户可访问，API鉴权+资源归属校验 |
| 声纹水印 | 合成音频嵌入不可听数字水印，可溯源到具体用户和模型 |
| 防滥用检测 | 同一声音模型合成异常频次检测（如短时间大量合成），触发人工审核 |
| 授权机制 | 用户首次使用声音克隆需签署《声音数据授权协议》，明确使用范围 |
| 删除权 | 用户可随时一键删除声音模型和原始录音，彻底清除数据 |
| 数据隔离 | 声音模型数据与其他业务数据物理/逻辑隔离存储 |

### 7.2 内容安全

| 层级 | 措施 |
|------|------|
| 预置内容 | 运营团队人工审核，建立分级标准 |
| AI生成文本 | 接入阿里云/百度内容安全API做文本审核，自建敏感词库二次过滤 |
| AI生成音频 | 合成后抽检，ASR转文本后再过一遍文本审核 |
| 用户举报 | 应用内举报入口，24小时内人工处理 |
| 儿童模式 | 严格白名单机制，仅展示审核通过的适龄内容 |

### 7.3 接口安全

| 措施 | 实现方式 |
|------|---------|
| 身份认证 | JWT Token + Refresh Token双Token机制 |
| 接口签名 | 敏感接口（支付、数据删除）增加请求签名防篡改 |
| 限流 | API网关+Redis双层限流，按用户ID/IP维度限流 |
| 防刷 | 录音上传/AI生成/合成接口增加验证码/行为验证 |
| 数据脱敏 | 日志中手机号脱敏、儿童信息脱敏 |

---

## 八、API接口设计（RESTful规范）

### 8.1 核心接口列表

#### 认证模块
```
POST   /api/v1/auth/sms/send          # 发送验证码
POST   /api/v1/auth/sms/login         # 手机号验证码登录
POST   /api/v1/auth/wechat/login      # 微信登录
POST   /api/v1/auth/apple/login       # Apple ID登录
POST   /api/v1/auth/refresh           # 刷新Token
DELETE /api/v1/auth/logout            # 退出登录
```

#### 家庭/孩子档案
```
GET    /api/v1/family/children        # 获取孩子档案列表
POST   /api/v1/family/children        # 创建孩子档案
PUT    /api/v1/family/children/:id    # 更新孩子档案
DELETE /api/v1/family/children/:id    # 删除孩子档案
```

#### 声音克隆
```
POST   /api/v1/voice/upload-url       # 获取录音上传预签名URL
POST   /api/v1/voice/models           # 创建声音模型（提交训练）
GET    /api/v1/voice/models           # 获取我的声音模型列表
GET    /api/v1/voice/models/:id       # 获取模型详情
POST   /api/v1/voice/models/:id/try   # 试听合成（30秒）
POST   /api/v1/voice/models/:id/append# 追加训练
DELETE /api/v1/voice/models/:id       # 删除模型
GET    /api/v1/voice/training/:task_id# 查询训练进度
```

#### 故事内容
```
GET    /api/v1/stories                # 故事列表（分页/筛选/搜索）
GET    /api/v1/stories/:id            # 故事详情
GET    /api/v1/stories/categories     # 分类列表
GET    /api/v1/stories/hot            # 热门故事
GET    /api/v1/stories/recommend      # 推荐故事
GET    /api/v1/stories/daily          # 今日推荐
POST   /api/v1/stories/:id/favorite   # 收藏故事
DELETE /api/v1/stories/:id/favorite   # 取消收藏
GET    /api/v1/stories/favorites      # 收藏列表
GET    /api/v1/stories/history        # 播放历史
```

#### TTS合成
```
WS     /api/v1/tts/stream             # WebSocket流式合成
POST   /api/v1/tts/try                # 试听合成（30秒片段）
POST   /api/v1/tts/batch              # 批量合成（离线下载）
GET    /api/v1/tts/tasks/:id          # 查询合成任务状态
```

#### AI故事生成
```
POST   /api/v1/ai-stories/generate    # 生成故事
POST   /api/v1/ai-stories/:id/regenerate # 重新生成
PUT    /api/v1/ai-stories/:id         # 编辑故事
GET    /api/v1/ai-stories             # 我的AI故事列表
GET    /api/v1/ai-stories/:id         # AI故事详情
POST   /api/v1/ai-stories/:id/save    # 保存到故事库
DELETE /api/v1/ai-stories/:id         # 删除AI故事
```

#### 播放与下载
```
POST   /api/v1/player/start           # 上报开始播放（计数）
POST   /api/v1/player/progress        # 上报播放进度
POST   /api/v1/player/complete        # 上报播放完成
GET    /api/v1/downloads              # 已下载列表
POST   /api/v1/downloads/:story_id    # 添加到下载队列
DELETE /api/v1/downloads/:story_id    # 删除下载
```

#### 支付与会员
```
GET    /api/v1/membership/plans       # 会员套餐列表
POST   /api/v1/orders                 # 创建订单
POST   /api/v1/orders/:id/callback    # 支付回调（各渠道）
GET    /api/v1/orders                 # 我的订单
GET    /api/v1/membership/status      # 会员状态
```

---

## 九、性能优化策略

### 9.1 TTS合成性能优化

| 优化项 | 方案 |
|--------|------|
| 模型推理加速 | TensorRT / ONNX Runtime优化模型推理；使用FlashAttention加速Transformer |
| 动态批处理 | Triton Inference Server的Dynamic Batching，合并多个推理请求 |
| 流式合成 | 文本按句切分，逐句合成流式返回，首包延迟<3秒 |
| 模型缓存 | 基础模型常驻GPU显存；LoRA权重存SSD，按需加载到显存（<500ms） |
| Speaker Embedding缓存 | 活跃用户的声音向量缓存在Redis和Worker本地内存 |
| 预合成 | 热门故事+活跃用户声音提前合成，用户点击即播 |
| GPU弹性伸缩 | K8s HPA根据GPU利用率和队列长度自动扩缩容合成Worker |

### 9.2 客户端性能优化

| 优化项 | 方案 |
|--------|------|
| 音频预加载 | 播放当前故事时预加载下一个故事的音频 |
| 智能预缓冲 | Wi-Fi下自动缓冲后续3个故事，蜂窝网络仅缓冲当前 |
| 本地缓存LRU | 最多缓存2GB音频，LRU淘汰策略 |
| 图片优化 | WebP格式、CDN裁剪合适尺寸、渐进式加载 |
| 启动优化 | 冷启动懒加载非首屏模块，首屏<3秒 |
| 包体积优化 | 资源动态下发、so库按需加载、iOS Bitcode/Android R8 |

### 9.3 服务端性能优化

| 优化项 | 方案 |
|--------|------|
| 多级缓存 | CDN → Redis → 本地缓存，减少DB查询 |
| 数据库优化 | 读写分离、分库分表（按user_id水平拆分播放历史等大表）、慢查询优化 |
| 异步处理 | 训练任务、合成任务、通知、计数等全部异步化，通过消息队列解耦 |
| 连接池 | 数据库/Redis/HTTP连接池复用，避免频繁建连 |
| 热点数据 | 热门故事详情、分类树等静态数据本地缓存 |

---

## 十、监控与运维

### 10.1 监控体系

```
┌─────────────────────────────────────────────────┐
│                 监控体系                          │
│                                                   │
│  ┌──────────────┐                                │
│  │ 基础设施监控  │  Prometheus + Grafana          │
│  │              │  - CPU/内存/磁盘/GPU利用率       │
│  │              │  - 网络IO/Pod状态               │
│  │              │  - K8s集群状态                  │
│  └──────────────┘                                │
│  ┌──────────────┐                                │
│  │ 业务监控      │  自定义Metrics                  │
│  │              │  - TTS合成QPS/延迟P99/成功率    │
│  │              │  - 训练任务排队数/耗时          │
│  │              │  - AI故事生成QPS/Token消耗      │
│  │              │  - 日活/播放时长/付费转化率     │
│  └──────────────┘                                │
│  ┌──────────────┐                                │
│  │ 日志聚合      │  ELK Stack                     │
│  │              │  - 业务日志/错误日志/审计日志    │
│  │              │  - 日志结构化(JSON格式)         │
│  └──────────────┘                                │
│  ┌──────────────┐                                │
│  │ 链路追踪      │  Jaeger / SkyWalking           │
│  │              │  - 全链路Trace ID               │
│  │              │  - 慢接口定位                   │
│  └──────────────┘                                │
│  ┌──────────────┐                                │
│  │ 告警          │  AlertManager + 钉钉/企微      │
│  │              │  - GPU利用率>90%持续5分钟       │
│  │              │  - TTS合成错误率>5%             │
│  │              │  - 合成队列积压>100             │
│  │              │  - P0接口P99延迟>3秒            │
│  └──────────────┘                                │
└─────────────────────────────────────────────────┘
```

### 10.2 关键业务指标监控

| 指标 | 告警阈值 | 说明 |
|------|---------|------|
| TTS合成成功率 | < 95% | 合成失败率过高 |
| TTS首包延迟P99 | > 5秒 | 用户等待太久 |
| 训练任务排队数 | > 50 | GPU资源不足 |
| AI故事生成成功率 | < 90% | LLM服务异常 |
| API接口P99延迟 | > 1秒 | 接口性能下降 |
| 音频CDN命中率 | < 85% | 缓存效率低 |
| 客户端崩溃率 | > 0.5% | App稳定性问题 |

### 10.3 CI/CD流水线

```
代码提交 → GitLab CI
    │
    ├── 代码检查 (Lint/静态分析)
    ├── 单元测试
    ├── 构建镜像 (Docker Build)
    ├── 镜像推送到Harbor仓库
    │
    ▼
ArgoCD 自动部署
    │
    ├── Dev环境 (自动部署)
    ├── Staging环境 (自动部署，自动化测试)
    └── Prod环境 (手动审批后部署，灰度发布)
```

### 10.4 GPU资源管理

| 策略 | 说明 |
|------|------|
| GPU节点池划分 | 推理节点池(A10/L40) + 训练节点池(A100)，训练节点弹性伸缩 |
| 时间分片 | 白天(7-23点)80%资源给合成推理，20%给训练；夜间相反 |
| 训练任务调度 | 低优先级训练任务在夜间(0-6点)批量执行，白天空闲GPU才训练 |
| 模型显存管理 | 基础模型常驻显存，LoRA权重按需加载/卸载，避免显存溢出 |
| Spot实例 | 训练Worker使用竞价实例，成本降低70% |

---

## 十一、MVP阶段技术实施建议

### 11.1 MVP技术范围（最小可行产品）

**第一阶段（1-2个月）：快速验证**

| 模块 | MVP方案 | 说明 |
|------|---------|------|
| 客户端 | iOS优先（SwiftUI），Android跟进 | iOS用户付费意愿更高，先做iOS |
| 声音克隆 | 接入商用API（如火山引擎/讯飞声音克隆） | 快速上线验证需求，不自研 |
| 故事内容 | 采购/录入500篇经典故事文本 | 运营团队准备内容 |
| TTS合成 | 商用API流式合成 | 快速接入，效果有保障 |
| AI故事 | 接入通义千问/文心一言API | Prompt工程+审核 |
| 后端 | 单体Go服务起步 | 快速迭代，不急于微服务拆分 |
| 数据库 | MySQL + Redis + OSS | 够用即可 |
| 部署 | 云服务器ECS + 容器化Docker Compose | 不上K8s，降低运维复杂度 |
| 支付 | Apple IAP | iOS先上 |

**第二阶段（3-4个月）：技术升级**

| 模块 | 升级方案 |
|------|---------|
| 声音克隆 | 切换到自部署CosyVoice，降低成本、提升效果 |
| 后端 | 拆分为核心微服务（用户/内容/声音/合成/AI故事） |
| 部署 | 迁移到K8s集群，引入GPU节点池 |
| 支付 | 接入微信支付、支付宝、Google Play |
| 监控 | 搭建Prometheus+Grafana+ELK监控体系 |
| Android | 完成Android版本开发上线 |

**第三阶段（6个月+）：规模化**

| 模块 | 升级方案 |
|------|---------|
| 声音克隆 | 自研模型微调，提升克隆相似度和情感表现力 |
| 推荐系统 | 引入协同过滤+深度学习推荐模型 |
| 性能优化 | 全链路性能优化，支撑百万级用户 |
| 多语言 | 支持英文、方言等多语言TTS |
| 硬件接入 | 对接儿童智能音箱/故事机SDK |

### 11.2 初期团队配置建议

| 角色 | 人数 | 职责 |
|------|------|------|
| 产品经理 | 1 | 需求管理、版本规划 |
| UI/UX设计师 | 1 | App界面设计、儿童模式设计 |
| iOS开发 | 2 | SwiftUI开发、音频模块 |
| Android开发 | 1-2 | Kotlin开发（可稍晚启动） |
| 后端开发 | 2-3 | Go/Java微服务、API、数据库 |
| AI算法工程师 | 2 | 声音克隆模型部署调优、LLM Prompt工程 |
| 运维/DevOps | 1 | K8s集群、CI/CD、监控 |
| 测试 | 1-2 | 功能测试、兼容性测试、音频质量测试 |
| 内容运营 | 1-2 | 故事内容采编、审核 |

### 11.3 初期成本估算（月度）

| 项目 | 配置 | 预估月费 |
|------|------|---------|
| 云服务器（业务服务） | 8核16G x 4台 | ¥3,000 |
| GPU服务器（TTS推理） | NVIDIA A10 x 2台 | ¥8,000 |
| GPU服务器（训练/弹性） | 按需竞价实例 | ¥2,000 |
| 对象存储OSS | 1TB存储+流量 | ¥500 |
| CDN | 音频分发，按流量 | ¥1,000-3,000 |
| 数据库 | MySQL RDS + Redis | ¥2,000 |
| LLM API | 按Token计费 | ¥1,000-3,000 |
| 商用声音克隆API（MVP期） | 按次计费 | ¥2,000-5,000 |
| 其他（监控/日志/短信等） | - | ¥1,000 |
| **合计** | - | **约¥20,000-30,000/月** |

> 注：MVP阶段使用商用API可省去GPU服务器成本，月成本可控制在¥10,000以内。

---

## 十二、技术风险与应对

| 风险 | 影响 | 概率 | 应对策略 |
|------|------|------|---------|
| 开源TTS模型中文效果不达预期 | 高 | 中 | MVP先用商用API保底；准备2-3个备选模型；持续收集bad case优化 |
| GPU资源成本过高 | 中 | 高 | 预合成缓存减少实时合成；Spot实例训练；模型量化压缩；合理定价覆盖成本 |
| 声音克隆被用于诈骗 | 高 | 低 | 数字水印溯源；合成频次异常检测；用户协议约束；配合监管 |
| LLM生成内容质量不稳定 | 中 | 中 | 多模型降级策略；Prompt优化；人工抽检；用户可编辑 |
| 高并发下合成队列积压 | 中 | 中 | GPU弹性伸缩；优先级队列；预合成热门内容；降级返回预置主播版本 |
| App端音频兼容性问题 | 中 | 中 | 充分测试主流机型；使用成熟播放框架(AVPlayer/ExoPlayer)；降级策略 |
| 用户隐私数据泄露 | 高 | 低 | 全链路加密；最小权限原则；安全审计；等保合规 |

---

## 十三、附录

### 13.1 推荐开源项目参考

| 项目 | 地址 | 用途 |
|------|------|------|
| CosyVoice | github.com/FunAudioLLM/CosyVoice | 声音克隆+TTS主力引擎 |
| XTTS v2 | github.com/coqui-ai/TTS | 多语言TTS备选 |
| ChatTTS | github.com/2noise/ChatTTS | 高表现力TTS参考 |
| RNNoise | github.com/xiph/rnnoise | 实时降噪 |
| WebRTC VAD | webrtc.googlesource.com | 语音活动检测 |
| Triton Inference Server | github.com/triton-inference-server | GPU推理服务 |
| vLLM | github.com/vllm-project/vllm | LLM推理加速 |

### 13.2 开发环境要求

| 环境 | 要求 |
|------|------|
| iOS开发 | macOS + Xcode 15+，iOS 14+真机调试 |
| Android开发 | Android Studio Hedgehog+，JDK 17，Android 8.0+真机 |
| 后端开发 | Go 1.21+ / Java 17+ / Python 3.10+ |
| AI开发 | Python 3.10+，PyTorch 2.0+，CUDA 12+ |
| 本地GPU | 建议NVIDIA GPU（RTX 3090/4090或A100）用于模型调试 |
| Docker | Docker 24+ + Docker Compose |
| K8s | 可使用云厂商托管K8s（ACK/GKE/EKS）|